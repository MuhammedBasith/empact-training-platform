import { Router } from "express";   
import { sendNotification } from "../controller/notification-service";
const routes=Router();

// routes.post('/send-notification',sendNotification);
export default routes;